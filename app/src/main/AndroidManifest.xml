<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nz.co.nonameden.spotifystreamer" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/title_activity_spotify"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.SpotifySearchActivity"
            android:label="@string/title_activity_spotify" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.SpotifyTopTracksActivity"
            android:label="@string/title_activity_spotify_tracks"
            android:parentActivityName=".ui.SpotifySearchActivity">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.SpotifySearchActivity" />
        </activity>

        <activity
            android:name=".ui.SpotifySettingsActivity"
            android:label="@string/title_settings"
            android:parentActivityName=".ui.SpotifySearchActivity">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.SpotifySearchActivity" />
        </activity>
    </application>

</manifest>
